services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_PASSWORD: password 
    ports:
      - "5432:5432"
    # network_mode: host
    volumes:
      - postgres:/var/lib/postgresql/data

volumes:
  postgres:
